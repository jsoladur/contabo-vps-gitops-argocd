apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: openclaw-ws
spec:
  headers:
    customRequestHeaders:
      Upgrade: "websocket"
      Connection: "Upgrade"
    hostsProxyHeaders:
      - "X-Forwarded-Host"
