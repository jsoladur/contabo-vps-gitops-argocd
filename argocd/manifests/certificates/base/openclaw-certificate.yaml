apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: openclaw-certificate
  labels:
    app.kubernetes.io/name: openclaw-certificate
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
    argocd.argoproj.io/sync-options: Prune=true
spec:
  secretName: openclaw-vps-jmsola-dev-cert-secret
  commonName: "openclaw.vps.jmsola.dev"
  issuerRef: 
    kind: ClusterIssuer
    name: letsencrypt-prod
    group: cert-manager.io
  dnsNames:
    - "openclaw.vps.jmsola.dev"
  secretTemplate:
    annotations:
      reflector.v1.k8s.emberstack.com/reflection-allowed: "true"
      reflector.v1.k8s.emberstack.com/reflection-allowed-namespaces: "openclaw"  # Control destination namespaces
      reflector.v1.k8s.emberstack.com/reflection-auto-enabled: "true" # Auto create reflection for matching namespaces
      reflector.v1.k8s.emberstack.com/reflection-auto-namespaces: "openclaw" # Control auto-reflection namespaces